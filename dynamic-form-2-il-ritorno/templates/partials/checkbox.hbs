<div class="checkbox">
    <h2>{{nome}}</h2>
    <fieldset>
    {{#each options}}
    <input type="checkbox" name="{{../nome}}[]" value = "{{this}}" />
    <label>{{this}}</label>
    {{/each}}
    </fieldset>
</div>